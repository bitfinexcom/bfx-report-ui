import hl from './HorizontalLine'

import ma from './MovingAverage'

import bb from './BollingerBand'

import bt from './BacktestTrades'

const { HorizontalLine, HorizontalLineForm } = hl
const { MovingAverage, MovingAverageForm } = ma
const { BollingerBand, BollingerBandForm } = bb
const { BacktestTrades, BacktestTradesForm } = bt

export default {
  HorizontalLine,
  HorizontalLineForm,
  MovingAverage,
  MovingAverageForm,
  BollingerBand,
  BollingerBandForm,
  BacktestTrades,
  BacktestTradesForm,
}
